{
  "name": "helmdesk-ui",
  "version": "1.0.0",
  "description": "HelmDesk Custom Page UI (embedded in GoHighLevel) — setup wizard, queue, Kanban board, ticket detail, dashboard, settings.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4174"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.8"
  }
}
