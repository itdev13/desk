# API origin the UI talks to (must match the API's BASE_URL).
VITE_API_URL=http://localhost:3020

# Product analytics / clickstream. Set to false to compile the UI with tracking off entirely.
# (The API's ANALYTICS_ENABLED + excluded-location list are also honored at runtime.)
VITE_ANALYTICS_ENABLED=true
